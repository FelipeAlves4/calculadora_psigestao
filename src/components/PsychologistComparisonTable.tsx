import { ArrowRight, ArrowUp } from 'lucide-react';
import { PsychologistComparisonRow } from '../types/psychologist';
import { formatCurrency, formatPercentage } from '../utils/formatters';

const format = (row: PsychologistComparisonRow, value: number) => row.format === 'currency' ? formatCurrency(value) : row.format === 'percentage' ? formatPercentage(value) : row.format === 'hours' ? `${value.toLocaleString('pt-BR')} h` : value.toLocaleString('pt-BR');
const Status = ({ status }: { status: PsychologistComparisonRow['status'] }) => status === 'positive' ? <span className="status-badge bg-emerald-500/15 text-emerald-300"><ArrowUp size={15} />Melhor</span> : status === 'negative' ? <span className="status-badge bg-red-500/15 text-red-300"><ArrowRight size={15} />Atenção</span> : <span className="status-badge bg-slate-700/70 text-slate-200"><ArrowRight size={15} />Neutro</span>;

export const PsychologistComparisonTable = ({ rows }: { rows: PsychologistComparisonRow[] }) => <><div className="comparison-mobile-list">{rows.map((row) => <article key={row.key} className="comparison-mobile-card"><div className="flex items-start justify-between gap-3"><h3>{row.label}</h3><Status status={row.status} /></div><div className="comparison-mobile-values"><div><span>Atual</span><strong>{format(row, row.current)}</strong></div><div><span>Projetado</span><strong>{format(row, row.projected)}</strong></div></div><div className="comparison-mobile-difference"><span>Diferença</span><strong className={row.status === 'positive' ? 'text-emerald-300' : row.status === 'negative' ? 'text-red-300' : 'text-slate-200'}>{format(row, row.difference)}</strong></div></article>)}</div><div className="comparison-table-shell"><table className="min-w-full text-sm"><thead><tr><th>Indicador</th><th>Cenário atual</th><th>Cenário projetado</th><th>Diferença</th><th>Resultado</th></tr></thead><tbody>{rows.map((row) => <tr key={row.key}><td className="font-semibold text-white">{row.label}</td><td>{format(row, row.current)}</td><td>{format(row, row.projected)}</td><td className={row.status === 'positive' ? 'text-emerald-300' : row.status === 'negative' ? 'text-red-300' : 'text-slate-300'}>{format(row, row.difference)}</td><td><Status status={row.status} /></td></tr>)}</tbody></table></div></>;
